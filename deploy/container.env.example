# Copy to .env.container for runtime only. Never pass secrets to docker build.
# These are placeholders; this file does not authorize any provider request.
LLM_API_KEY=replace-at-runtime
LLM_BASE_URL=https://provider.example.invalid/v1
LLM_MODEL=replace-at-runtime
LLM_TIMEOUT_SECONDS=60
